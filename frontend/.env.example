VITE_API_URL=http://localhost:8000/api
# Optional Bearer token for protected API (must match backend API_TOKEN)
VITE_API_TOKEN=
# For docker internal proxy target (frontend container -> backend)
# VITE_PROXY_TARGET=http://backend
